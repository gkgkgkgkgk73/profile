export interface ProjectType {
    name: string;           // project name
    picture: string[];
    description: string;    // project description
    fetool: string;         // frontend tool
    betool: string;         // backend tool
    role: string;           // my role in the project
    period: string;         // period progressing the project
    gain: string;           // gain through the project
}